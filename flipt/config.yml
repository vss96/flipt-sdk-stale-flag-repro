log:
  level: DEBUG

authentication:
  required: true
  methods:
    token:
      enabled: true
      bootstrap:
        token: test-token-123
    jwt:
      enabled: true
      public_key_file: /etc/flipt/keys/public.pem
      validate_claims:
        issuer: flipt-identity-server
